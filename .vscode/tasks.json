{
    "version": "2.0.0",
    "tasks": [
        // Scripts do front
        {
            "label": "front docker-compose up",
            "type": "shell",
            "command": "docker-compose",
            "args": ["-f", "app/docker-compose.yml", "up", "--build", "-d"]
        }
    ]
}
